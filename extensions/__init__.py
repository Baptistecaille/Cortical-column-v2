"""
Extensions biologiques — Phase 3 (optionnelles).

Ces modules enrichissent le modèle de base mais ne doivent être
ajoutés qu'après validation des invariants de Phase 1.

Ordre recommandé (CLAUDE.md §7) :
    1. pe_circuits.py  — PRIORITÉ 1 (change la nature du calcul)
    2. stp_synapse.py  — iSTP (PV STD, SST STF)
    3. astrocyte.py    — mémoire associative
    4. linoss.py       — ODE stable
    5. pac_detector.py — couplage amplitude-phase temporel
"""
